apiVersion: v1
kind: Service
metadata:
  name: order-service
spec:
  selector:
    app: order-service     # routes to all Pods carrying this label
  ports:
    - port: 80             # the Service's port
      targetPort: 8080     # the container's port